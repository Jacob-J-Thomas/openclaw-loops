# Delivery status

Active scope: all requirements in [DELIVERY_PLAN.md](DELIVERY_PLAN.md), including expansion milestones.

## Baseline

- Source: 0.3.0, OpenClaw 2026.9.3. All initial project files were untracked; no baseline source was overwritten without a private archive.
- Before-work archive: `.dev-profile/backups/source-0.3.0-before-release-work.tgz` (local only).
- Existing evidence: 61 deterministic tests from the audit; earlier packaged and real command/tool/UI POC checks in `evidence/` and `docs/VERIFICATION.md`. These do not establish 1.0 readiness.

## Progress

| Requirements | State | Evidence / next gate |
|---|---|---|
| R01 | Implemented; local profile verification | Recognizable generated policy repair with backups and repeatable setup; custom-policy regressions. |
| R02/R03/R18 | Partial; source and live evidence | Advanced descriptors, explicit zero, inherited generation/model/reasoning settings, preflight and attribution. V2 timeout inheritance added. Host runtime/account parity and unsupported sampling require SDK01/02. Live Codex and Ollama calls proven; model/provider matrix remains. |
| R04 | Partial | Main-agent-only filter removed; trusted host session checks retained. Stable principal and reset/history authority require SDK03. |
| R05 | Partial; storage/provider diagnostics verified | Alpha.12 classifies native storage failures and wrapped provider errors without copying private payloads into run history. Actual SDK tests cover UI/tool/command errors and rejected writes before inference; real Linux filesystem-full checks return the structured storage code. A live unavailable-model failure retains the overridden model and failed node. The native inspector now displays code, phase, node/model, retryability and recovery. Unknown errors remain generic; wider provider/account coverage remains. |
| R06/R09 | Implemented; regression evidence | Producer fields, unused truthy operand, fresh intentional command IDs and canonical retry fingerprints. Pruned admission identities survive restart and cannot execute again in worker/SDK tests. Live rejection messaging passes after restart in alpha.7. |
| R07/R08 | Partial; regression and process-fault evidence | Commit rollback keeps active run references consistent; unreadable rollback stops unverified execution. Alpha.10 covers real SQLite FULL/automatic rollback, failed rollback, SIGKILL before/after COMMIT and immutable revision/grant preservation. Alpha.11 also verifies real Linux tmpfs exhaustion and recovery on Node 24/26; power-loss and broader physical-failure acceptance remain. |
| R10 | Implemented backend; partial UI | Immutable versions, draft/publication, restore/archive/delete/recover; version UI. Archive/recovery UI and three-way conflict merge implemented; a real concurrent-edit conflict/merge preserves both authors' edits and the enabled publication. Expanded host-role acceptance remains. |
| R11/R12 | Partial; migration, crash recovery and lazy history verified locally | SQLite worker/WAL/integrity/backup, exact legacy import, attempt evidence and explicit recovery ancestry. Alpha.10 fixes competing stale-lock reclaimers with a lifetime SQLite lease and verifies 12 engine SIGKILL checkpoints. Alpha.11 commits run-only checkpoints; alpha.13 removes the worker history cache. Alpha.15 also keeps cold history out of the engine, while preserving parked runs, pinned revisions and held execution references through rollback. A 1,000-run fixture measured main heap after open at 7.1 MB versus 100 MB with exact full-output/integrity checks. Synchronous commit barriers, durable wake and wider fault coverage remain. |
| R13/R14/R16 | Partial | Configurable queued concurrency, physical cleanup accounting, no 20/50 record caps, paged Unicode outputs/history, immutable result documents and chunked input uploads. Alpha.15 queries history pages through the SQLite owner/time index and loads full cold runs only when authorized. Explicit age/count retention, unchanged-preview checks and admission tombstones implemented; actual UI/agent cleanup passed. Retention scans compact metadata to protect ancestry. Broader scale, document maintenance and physical-failure soak remain. |
| R15 | Partial | V2 nested JSON, zero-input graphs, safe traversal, typed equality and larger explicit budgets. Operator definition/input/prompt budgets and new-graph defaults now configure through the manifest; saved evidence stays readable after budget reductions. Alpha.13 adds one exhaustive internal node registry for schemas, input bindings, output fields, capability declarations, execution and editor metadata/defaults. The definition schema is byte-for-byte unchanged; a combined registry-authored test covers all nine kinds, six iterations, zero-valued inference settings, restart and both human-review exits. Larger-scale and further schema validation remain. |
| R17 | Partial; command parity verified | 31 agent tools and all 33 shared operations through conversation JSON commands, schema-derived help, concrete output schemas and explicit human review. Alpha.14 passes 65 real lifecycle commands per Gateway, including 50,000-byte staged input/full output, and fresh Codex/Ollama agent-tool invocation. Host-altered command input is rejected before dispatch; the public host's 4,096 UTF-16 inline-command limit has a chunked-upload path. Broader identity/role coverage remains. |
| R19–R21 | Partial | Undo/redo, node duplication, layout, binding picker, search, draft recovery, version controls, unpublished tests and explicit recovery. Alpha.16 fixes Undo to a saved definition leaving Run disabled and an obsolete recovery draft; native Reset/Undo/Redo/Undo, real invocation and reload pass. Other/newer drafts remain recoverable. Conflict merge, unfinished local drafts and archive recovery have prior live evidence; remaining authoring/a11y journeys and broader scale remain. |
| R22 | Partial | Empty invalidation events followed by authorized fetch. Durable requester-bound delivery needs SDK04. |
| R23 | Partial | Keyboard authoring controls, contrast/theme/responsive CSS, native Advanced zero-value UI proof. Alpha.7 keyboard create/enable/merge and 390 px authoring pass. Alpha.9 now follows host Light/Dark/System selection through inherited color-scheme, with measured text/placeholder contrast and real Advanced controls. Full screen-reader, browser and device acceptance remains. |
| R24 | Partial | Alpha.16 passes 171 source tests on macOS and a local Linux arm64 VM with Node 24.16.0/26.1.0, plus typecheck/lint/build and 31 extracted-package SDK tests. Compiled files match across platforms. Both installed profiles match the archive; every application table was unchanged across upgrade and original rows remain identical after acceptance. Fresh UI, command and agent-tool inference passes. Alpha.15's 65-command journeys and 103-run cold-history UI evidence remain separately recorded for the unchanged backend/worker. Real filesystem-full recovery passed on both Linux Node versions. Hosted CI, x64, deployment roles, broader resources/soak/upgrade acceptance remain. |
| R25 | Partial; local validation/dry run passed | MIT license, 27 dependency notices, contributor/deployment guides, pinned CI workflow, installable alpha tarball and upstream gap contracts. Exact alpha.15 artifact static validation against OpenClaw 2026.9.3 passes with zero findings; source-aligned alpha.13 and alpha.15 candidates passed code-plugin publication dry runs with ClawHub 0.23.3. No authentication/upload occurred; publisher identity, public source/CI, publication and fresh registry installation remain. Public GitHub source creation/push was rejected by automatic approval review; specific payload approval is pending. |
| X01–X29 | Pending | All expansion milestones remain in the active goal. |

Live details and their limitations: [VERIFICATION.md](VERIFICATION.md). Public host dependencies: [UPSTREAM_REQUIREMENTS.md](UPSTREAM_REQUIREMENTS.md). These are documented requirements, not submitted or accepted upstream changes.

Release status: **not ready, not published, goal incomplete**. Update rows only with actual evidence; distinguish implementation from live verification.
